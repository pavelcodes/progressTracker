<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.pavelplakhotny_c196">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.courses.addCourse.CourseAddActivity"
            tools:ignore="Instantiatable" />
        <activity android:name=".ui.assessments.assessmentAdd.AddAssessmentActivity"
            tools:ignore="Instantiatable" />
        <activity
            android:name=".ui.terms.addTerm.TermAddActivity"
            tools:ignore="Instantiatable" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            tools:ignore="Instantiatable">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver android:name=".CourseBroadcast" />
        <receiver android:name=".AssessmentBroadcast" />
    </application>

</manifest>